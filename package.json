{
  "name": "card-shuffling",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "idyll index.idyll --defaultComponents=components/default --theme idyll --css styles.css --template _index.html --watch",
    "build": "idyll index.idyll --defaultComponents=components/default --theme idyll --css styles.css --template _index.html",
    "deploy": "npm run build && gh-pages -d ./build"
  },
  "dependencies": {
    "d3": "^4.0.0",
    "d3-svg-annotation": "^2.2.5",
    "idyll": "^2.0.0",
    "idyll-d3-component": "^2.0.0"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
